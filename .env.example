# API Keys (Required)
# Get your OpenAI API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# === BINANCE API KEYS ===
# IMPORTANT: Use different keys for live trading vs testnet!

# Binance TESTNET API Keys (for testing with fake money)
# Get testnet keys from: https://testnet.binance.vision/
# These keys work ONLY on testnet and cannot access real funds
BINANCE_TESTNET_API_KEY=your-binance-testnet-api-key-here
BINANCE_TESTNET_SECRET_KEY=your-binance-testnet-secret-key-here

# Binance LIVE API Keys (for real trading with real money)
# Get live keys from: https://www.binance.com/en/my/settings/api-management
# WARNING: These keys can access real funds! Keep them secure!
BINANCE_LIVE_API_KEY=your-binance-live-api-key-here
BINANCE_LIVE_SECRET_KEY=your-binance-live-secret-key-here

# Optional: Get CoinGecko Pro API key from: https://www.coingecko.com/en/api/pricing
COINGECKO_API_KEY=your-coingecko-api-key-here

# Trading Configuration
TRADING_INTERVAL=300                # Seconds between trading cycles (300 = 5 minutes)
MAX_PORTFOLIO_RISK=0.05            # Maximum risk per trade (0.05 = 5%)
STOP_LOSS_PERCENTAGE=0.05          # Stop loss threshold (0.05 = 5%)
TAKE_PROFIT_PERCENTAGE=0.10        # Take profit target (0.10 = 10%)
MAX_TRADES_PER_DAY=10             # Maximum trades per day
MIN_TRADE_AMOUNT=10.0             # Minimum trade amount in USDT
MAX_TRADE_AMOUNT=100.0            # Maximum trade amount in USDT

# Risk Management
MAX_OPEN_POSITIONS=3              # Maximum concurrent positions
EMERGENCY_STOP_LOSS=0.15          # Emergency portfolio stop loss (0.15 = 15%)

# AI Configuration
AI_MODEL=gpt-4o-mini              # OpenAI model (gpt-4, gpt-4-turbo, gpt-3.5-turbo)
AI_TEMPERATURE=0.3                # AI response creativity (0.0-1.0, lower = more conservative)

# Exchange Configuration
USE_SANDBOX=true                  # Start with sandbox/testnet mode (recommended)
EXCHANGE=binance                  # Currently only supports binance

# Logging
LOG_LEVEL=INFO                    # Log level (DEBUG, INFO, WARNING, ERROR)
LOG_FILE=trading_bot.log          # Log file name 